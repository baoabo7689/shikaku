
=====================================================================================
D:
cd D:\Ps\BB\shikaku
yarn
yarn run dev 


=====================================================================================
Shikaku [Rectangles]
The Rules
	Match the Number: Every rectangle must contain exactly one number, 
	and the area of the rectangle (in grid cells) must equal that number.

	Fill the Grid: Every single cell on the grid must belong to a rectangle.
	No Overlaps: Rectangles cannot overlap or leave empty gaps.
	Shape Limits: Regions must be perfect rectangles or squares.


Solving Techniques
	1. Start with Prime Numbers
	Numbers that are prime (like 2, 3, 5, 7, 11, 13) 
	only have one possible rectangular dimension: 1 × N. 
	For example, a 5 must be a straight line of 5 cells. 
	This makes them highly restrictive and usually the best place to start.

	2. Evaluate Corners and Edges
	A corner clue is trapped by the grid's boundaries, severely limiting its possible shapes. 
	Look at clues nestled in corners and calculate if their allowed shapes can fit without clipping out-of-bounds.

	3. Large Numbers Partition the Grid
	Large clues in a grid leave limited room for other rectangles. 
	You can often test the 1 × N or N × M configurations for large numbers to see which ones break or box in other adjacent clues.

	4. Forced Shapes and Unreached Cells
	Scan the grid for isolated cells. 
	If an empty cell can only be reached by one specific number 
	(because all other neighbors are blocked or too far away), 
	you can instantly map out the dimensions of that number's rectangle.

	5. The Process of Elimination
	Because the entire grid must be filled without overlaps, 
	drawing one rectangle rules out those cells for all other numbers. 
	As the grid fills up, the remaining unassigned spaces will force the final shapes to lock into place.


https://shikaku.ch/guide.html
	Look for 1s and 2s






=====================================================================================
suggest some techniques to solve shikaku  puzzle
[đánh số cho từng rectangle ứng với thứ tự clue xuất hiện]
[mark ô nào thuộc về vùng nào]
[chọn 5 màu, tô từng vùng 1 màu]
	Start with clues near corners and edges.
	Generate possible rectangles for large clues.
	Eliminate candidates that overlap other clues.
	Solve forced rectangles.
	Recompute possibilities.
	Use area accounting and bottleneck analysis.
	Apply contradiction only if necessary.





=====================================================================================


learn from code D:\Ps\BB\sudoku [this is for sudoku game with different rules]
make shikaku game to folder D:\Ps\BB\shikaku
with rules as in the doc: D:\Ps\BB\shikaku\docs\rules.txt
this include:
WidthxHeight input with 2 textbox
Color: [4 colors] + No [this will use for solve game or add some hints]
Init section: Random [only 1 random, not separate by level], Import, Manual
Functional: Validate, Export, Block Init, Solve
Cell Model with: cell color [which color it should belong to]

Validation base on rules.txt
Solution base on idea:
	0. we mark each clue number with group number index 
	1. with each cell have number on it and it in the corner 
	[mean 3 cell up-left or up-right or down-left or down-right] already grouped to another group,
	it should have different number.
	And if it is prime, we can sure that it create rectangle px1,
	then we spread from it to form rectangle px1 with cells have same group number X
	
	If it not prime, we can calculate minimum factor F of it then we sure square 
	FxF from it is same group number with it. In this case we can calculate 
	distance from it to cell in same row/column to eleminate cases with facor from/greater than that distance
	
	2. If cell not in corner position, we calculate minimum factor F of it
	and distance D from it to cell in same row/column, if D < F then it should spread to other direction
	with minimum length [F-D], then cells from [it to that length same group number X]
	
	3. If maximum factor F of one cell cannot reach to next cell in [same row/column] and there no number between those cells 
	then length from next cell to minimum D-F is belong to next cell group.

	4. if one area not include number cell hint, it should belong to adjacent area,
	we should find out area is missing cells with area as it. then it belong to that area.
	
	
	

WidthxHeight input with 2 textbox, in first section
Random [only 1 random, not separate by level]


decrease button width,
puzzle grid must align top as message box
rearrange buttons as exact with:
Init section: Random, Import, Manual
Functional: Validate, Export, Block Init, Solve

decrease buttons size, grid cell size and make grid should align top same as message area box

maybe some area next together have same color, 
we should update color by loop through areas then choose color not same with it adjust areas


I still see 2 adsendent have same color
Maybe 5 colors filling need some algorithm.
So choose 6 colors to fill, maybe it will be simpler.



=====================================================================================


we can extend this puzzle by: add black color area as some hints in the init.
then in solution steps we should calculate corners/edges base on hints/solved cells







=====================================================================================







